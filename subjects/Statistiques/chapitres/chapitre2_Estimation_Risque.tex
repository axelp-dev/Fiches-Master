\minitoc 


% ===================================================================================================================
% Introduction

En statistiques paramétriques, on cherche à déterminer la valeur d'un paramètre 
$ \theta^*$ inconnu. Nous posons ici une autré demarche que celle des estimateurs vue 
précédement. 

% ===================================================================================================================
% Risque 

\section{Risque}

\begin{definition}[Règle de décision]
    On appelle \emph{règle de décision} $ \delta$ une statistique à valeurs dans un
    espace $(\D, \mathcal{D})$ telle que : 
        $$ \delta : ( \mathcal{X}, \mathcal{A})^{\oplus n} \longrightarrow (\D, \mathcal{D}). $$ 
\end{definition}

\begin{remark}
    Une règle de décision est donc une fonction mesurable et indépendante d'un paramètre $ \theta$ puis 
    c'est une statistique. 
\end{remark}

\begin{proposition}
    Il existe différents types de règles de décisions : 
    \begin{itemize}
        \item \textbf{Estimation} : On choisit un paramètre $ \theta$ par un 
        estimateur $ \delta(X_1, \dots, X_n)$ à partir d'informations contenues 
        dans un échantillon. On a donc $ \D = \Theta$ et on peut définir 
        \emph{l'erreur quadratique moyenne} : 
            $$ R_Q( \delta, \theta) = \mathbb{E}_\theta( \| \delta - \theta\|_p^2) $$
        sous $ \myP_\theta$. Ici, on aura $ \D = \mathcal{P}( \Theta)$. 
        \item \textbf{Localisation} : Ici, on choisit plutôt une région pouvant contenir 
        $ \theta$. On veut donc une notion de \emph{confiance} telle que : 
            $$ \forall \theta \in \Theta, \quad \myP_\theta ( \theta \in \delta(X_1, \dots, X_n)) \geqslant 1 - \alpha. $$
        \item \textbf{Test} : On a ici $ \D = \{0,1\}$ ou $ \D = [0,1]$. On cherche 
        à vérifier si une affirmation sur $ \theta$ est vraie plutôt qu'une autre. 
        Elles seront définies par deux types d'hypothèses : \emph{l'hypothèse nulle} $ \mathcal{H}_0$ et 
        \emph{l'hypothèse alternative} $ \mathcal{H}_1$.
        On souhaite donc que : 
            \begin{itemize}
                \item $ \myP( \delta(X_1, \dots, X_n) = 0) $ soit proche de $1$ si $ \theta \in \Theta_0$. 
                \item $ \myP( \delta(X_1, \dots, X_n) = 1) $ soit proche de $1$ si $ \theta \not \in \Theta_0$. 
            \end{itemize} 
    \end{itemize}
\end{proposition}

% ===================================================================================================================
% Estimateurs 

\section{Estimateurs}

\subsection{Définition}

On cherche ici à estimer une transformation $g( \theta)$ d'un paramètre $ \theta$ 
connu. Pour cela, on dispose de : 
    $$ g : 
        \begin{cases}
            \Theta \subseteq \R^p \longrightarrow \R^q \\ 
            \theta \longmapsto g( \theta)    
        \end{cases}
    $$

\begin{definition}[Estimateur]
    On appelle \emph{estimateur} de $g( \theta)$ toute fonction mesurable : 
        $$ \delta : ( \mathcal{X}, \mathcal{A})^{ \oplus n} \longrightarrow (g(\Theta), \mathcal{D}) $$ 
    qui ne dépend pas de $ \theta$. On note $ \Delta$ l'ensemble des estimateurs de 
    $ \theta$. 
\end{definition}

\begin{remark}
    Si $ \hat{ \theta}_n$ est un "bon" estimateur de $ \theta$, alors $ g( \hat{ \theta}_n)$ 
    n'est pas nécessairement un aussi bon estimateur pour $ g( \theta)$. La composition 
    doit s'étudier au cas par cas. 
\end{remark}

\subsection{Comparaisons}

On souhaite qu'un estimateur se comporte comme une boîte noire pointant toujours 
sur $ \theta$ peu importe sa valeur. 

\begin{definition}[Estimateur consistant]
    Une suite d'estimateurs $ \delta_n \in \Delta^\N$ sera dite \emph{consistante}
    si : 
        $$ 
            \forall \theta \in \Theta, \quad 
            \lim_{n \to \infty} \delta_n(X_1, \dots, X_n) = g( \theta) \quad \myP_\theta \text{- p.s}.
        $$
\end{definition}

\begin{definition}[Estimateur Asymptotiquement Gaussien]
    Un suite d'estimateurs $ \delta_n \in \Delta^\N$ sera dite 
    \emph{asymptotiquement gaussienne} si pour tout $ \theta \in \Theta$, il existe 
    une matrice $ V_\theta$ semi-définie positive telle que sous $P_\theta^\infty$, 
    on ait : 
        $$ \sqrt{n} ( \delta_n(X_1, \dots, X_n) - g(\theta)) 
            \underset{n \to \infty}{\overset{\mathcal{N}}{\longrightarrow}} \mathcal{N}(0_p,\V_\theta). 
        $$
\end{definition}

\subsection{Comparaison par le risque}

Un autre façon de comparer deux estimateurs est d'introduire une notion de 
\emph{perte} qui quantifie le degré d'erreur d'estimation de $ g( \theta)$. 
Pour cela, on définit les fonctions de perte ou \emph{loss} en anglais. 

\begin{definition}[Fonction de perte]
    Une fonction de perte $ \ell$ est une fonction mesurable positive : 
        $$ \ell : \Theta \times g(\Theta) \longrightarrow (\R_+, \mathcal{B}_\R) $$ 
    telle que : 
        $$ \boxed{ \forall ( \theta, d) \in \Theta \times g( \Theta), \quad 
            l( \theta, d) = 0 \iff d = g( \theta)    
        }.
        $$
\end{definition}

Comme dit précédemment, on souhaite que nos estimateurs se comportent 
comme des boîtes noires. Il nous faut cela minimiser la perte moyenne 
d'une décision pour toutes les valeurs de $ g(\theta)$ possibles. 
Cette notion sera donc définie comme le \emph{risque}. 

\begin{definition}[Risque]
    On définit la \emph{fonction de risque} associée à une décision $ \delta \in \Delta$, 
    comme l'application : 
        $$ \theta \longmapsto R( \theta, \delta) = \mathbb{E}_\theta( \ell( \theta, \delta)).$$
    Ainsi, un estimateur $ \delta$ sera dit \emph{préférable} à un autre $ \delta' \in \Delta$, ssi : 
        $$ \forall \theta \in \Theta, \quad R( \theta, \delta') \leqslant R( \theta, \delta).$$
    On notera alors $ \delta \succeq \delta'$. 
\end{definition}

\begin{remark}
    Se pose alors un problème fondamental dans la recherche de l'estimateur optimal. 
    Admettons que vous ayez un ami complotiste qui croit les extraterrestres sont 
    parmi nous. Dans tous les mondes qu'il pourrait exister, la plupart ne sont pas 
    dirigés par une secte martienne. Or, il suffit qu'un seul monde le soit pour 
    que vous ne soyez pas meilleur que votre ami pour une valeur spécifique de $ \theta$. 

    C'est exactement le problème que posent les estimateurs constants. On va donc chercher 
    à les exclure en créant des sous-classes $ \Delta_i \subset \Delta$. 
\end{remark}

\begin{proof}
    Supposons qu'il existe $ \delta^* \in \Delta$ un estimateur parfait tel que : 
        $$ \forall \theta \in \Theta, \quad \forall \delta \in \Delta, \quad 
            R(\theta, \delta^*) \leqslant R( \theta, \delta). 
        $$
    alors $ \delta = \delta_{\theta_0}$ qui répond $ \theta_0$ presque-partout 
    quelque soit l'échantillon proposé (un estimateur constant). 
    D'où : 
        $$ R( \theta, \delta_{\theta_0})
            \begin{cases}
                = 0 & \text{ si } \theta = \theta_0 \\ 
                > 0  & \text{ si } \theta \not = \theta_0.
            \end{cases}
        $$ 
    Donc nécessairement $ \forall \theta \in \Theta, R(\theta, \delta^*) = 0$ pour 
    faire mieux que chacun des estimateurs constants. 
\end{proof}

\begin{definition}[Estimateurs admissibles]
    On définit l'ensemble des estimateurs \emph{admissibles}, noté $ \Delta_0$, comme l'ensemble 
    des estimateurs "meilleurs en moyenne". Plus formellement, 
        $$ \Delta_0 = \{ \delta \in \Delta_0 \mid \not \exists \delta' \in \Delta_0, \delta' \succeq \delta\}. $$
\end{definition}

\begin{proposition}
    Supposons que $ \Delta_0$ est stable par combinaison convexe et que $\ell$ est strictement 
    convexe en la décision $ \delta$. Alors si $dd_0$ est admissible dans $ \Delta_0$ 
    et si $ \delta_1$ admet la même fonction de risque, alors $ \delta_1 = \delta_0$ $ \myP_\theta$-p.p 
    pour tout $ \theta \in \Theta$. 
\end{proposition}

\begin{example}
    Soit $ \Delta_0$ la classe des estimateurs sans biais moyen : 
        $$ \Delta_0 := \{ \delta \in \Delta \mid \mathbb{E}_\theta( \delta(X_1, \dots, X_n)) = g( \theta)\}. $$ 
    Soit $ \delta_0, \delta_1 \in \Delta_0$, alors : 
        $$ \forall \alpha \in [0,1], \quad \alpha \delta_0 + (1 - \alpha) \delta_1 \in \Delta_0 $$
    par linéarité de $ \mathbb{E}_\theta(\cdot)$. Donc $ \Delta_0$ est bien stable et : 
        $$ \delta \longmapsto \ell(\theta, \delta) = \| g( \theta) - \delta \|_2^2 $$
    est convexe. Donc dans la classe des estimateurs sans biais, s'il existe un estimateur 
    de plus petit risque, alors il est unique. 
\end{example}

\subsection{Optimalité}

On cherche donc à déterminer des estimateurs optimaux (i.e minimisant la fonction 
de risque). 

\begin{definition}[Estimateur Optimal]
    On dira que $ \delta_{opt}$ est $\ell$-optimal dans $ \Delta_0$ si : 
        $$ \forall \theta \in \Theta, \quad R( \theta, \delta_{opt}) = \min_{ \delta \in \Delta_0} R( \theta, \delta). $$ 
\end{definition}

\begin{definition}[Estimateur Asymptotiquement Optimal]
    On dit qu'une suite $ \delta_n(X_1, \dots, X_n)$ est asymptotiquement 
    $\ell$-optimale relativement à une suite $ \Delta_n$ de classes d'estimateurs si : 
        $$ \forall \theta \in \Theta, \quad \lim_{n \to \infty} 
        \frac{R(\theta, \delta_n(X_1, \dots, X_n))}{\displaystyle\inf_{ \delta \in \Delta_n} R(\theta, \delta)} = 1. $$
\end{definition}

\subsection{Biais}

La notion de biais permet de quantifier la façon dont un estimateur $ \delta$ approche 
$ g(\theta)$ en moyenne. Pour cela, il existe différents types de biais. 

\begin{definition}[Biais Moyen]
    On dira qu'un estimateyr $ \delta \in \Delta$ est \emph{sans biais moyen} pour $g(\theta)$ 
    si : 
        $$ \mathbb{E}_\theta( \delta(X_1, \dots, X_n)) = g(\theta).$$ 
    On peut ainsi définir la \emph{fonction de biais} $b$ d'un estimateur : 
        $$ 
            b : \theta \in \Theta \longmapsto \mathbb{E}_\theta(\delta) - g(\theta). 
        $$ 
\end{definition}

De même que pour l'optimalité, il est souhaitable que le biais d'un estimateur 
diminue lorsque la taille de l'échantillon augmente. On parle alors de \emph{biais moyen asymptotique}. 

\begin{definition}[Biais moyen asymptotique]
    Un estimateur $ \delta$ est dit \emph{sans biais moyen asymptotique} si : 
        $$ \forall \theta \in \Theta, \quad 
            \lim_{n \to \infty} b(\delta_n) = 0. $$
    Ce qui revient à $ \mathbb{E}_\theta( \delta_n) \underset{n \to \infty}{\longrightarrow} g(\theta)$. 
\end{definition}

\begin{remark}
    Un estimateur centré n'est pas nécessairement sans biais moyen. La notion de centrage exprime 
    seulement le fait que $ \mathbb{E}_\theta( \delta(X_1, \dots, X_n)) = 0_q $ pour tout 
    $ \theta \in \Theta$. Or, si $ g(\theta) \not = 0_q$ pour tout $ \theta$, alors 
    l'estimateur est biaisé. 
\end{remark}

\begin{definition}[Biais Général]
    Un estimateur $ \delta$ est dit \emph{sans biais général} pour une fonction de perte $ \ell$ si : 
        $$ \forall \theta, \theta' \in \Theta, \quad 
            R(\theta, \delta) = \mathbb{E}_\theta( \ell(\theta, \delta)) \leqslant \mathbb{E}_\theta(\ell(\theta', \delta)).
        $$
    On notera $ \Delta_{sb}$ la classe des estimateurs sans biais. 
\end{definition}

\begin{definition}[Biais Médian]
    Si $ q = 1$, un estimateur $ \delta \in \Delta$ est \emph{sans biais médian} si : 
        $$ \forall \theta \in \Theta, \quad 
            \begin{cases}
                \myP_\theta( \delta(X_1, \dots, X_n) > g(\theta)) \leqslant 1/2 \\
                \myP_\theta( \delta(X_1, \dots, X_n) < g(\theta)) \leqslant 1/2
            \end{cases}.
        $$
\end{definition}

La notion de biais médian signifie qu'un estimateur a autant de chances de surestimer $ g(\theta)$ que 
de le sous-estimer. Elle traduit de façon plus fine la dispersion possible de l'estimateur 
autout de $ g(\theta)$. 

\begin{proposition}
    % TODO : Ajouter lien entre les biais via fonction de perte. 
\end{proposition}


% ===================================================================================================================
% Risque Quadratique 

\section{Risque Quadratique}

La notion de biais vue précédement traduit le fait qu'un estimateur soit en moyenne 
"précis" ou pas pour estimer $ g(\theta)$, mais elle ne donne aucune notion sur 
la dispersion d'un estimateur autout du paramètre. Or, il serait parfois préférable 
de travailler avec un estimateur moins "précis" (biaisé) mais moins dispersé. 

\subsection{Cas $q = 1$}

On cherche donc une mesure permettant d'additionner la notion de biais et de 
variance d'un estimateur. En effet, un estimateur sans biais peut être complètement 
divergent pour $n$ grand (non consistant) et inversement. 

Au lieu d'utiliser une métrique différente, on pourrait adapter celles 
déjà introduites telles que la \emph{perte} et le \emph{risque moyen}. Une bonne fonction 
de perte pourrait être la \emph{perte quadratique} : 
    $$ \ell_Q(\theta, \delta) = ( \delta - g(\theta))^2 $$ 
possédant de bonnes propriétés de convexité et de différentiabilité. De plus elle pénalise 
de façon égale les erreurs négatives ou positives tout en pénalisant fortement les grosses erreurs. 

\begin{definition}[Risque Quadratique Moyen]
    On définit le \emph{risque quadratique moyen} $ R_Q$ d'un estimateur $ \delta$ 
    comme le risque de cet estimateur appliqué à la fonction de perte quedratique. 
    D'où : 
        $$ \forall \theta \in \Theta, \quad 
            R_Q(\theta, \delta) = \mathbb{E}_\theta(\ell_Q(\theta, \delta)) = \mathbb{E}_\theta((\delta - g(\theta))^2). 
        $$
\end{definition}

\begin{prop}[Décomposition]
    Comme souhaité, le risque quadratique moyen vérifiera : 
        $$ \forall \delta \in \Delta, \forall \theta \in \Theta, \quad 
            \boxed{ R_Q(\theta, \delta) = \V_\theta(\delta) + b(\theta, \delta)^2}.
        $$
\end{prop}

\begin{proof}
    Soient $ \delta \in \Delta$ et $ \theta \in \Theta$, on a : 
        \begin{align*}
            R_Q(\theta, \delta) &= \mathbb{E}_\theta((\delta - g(\theta))^2) \\ 
            &= \mathbb{E}_\theta((\delta - \mathbb{E}_\theta(\delta) + \mathbb{E}_\theta(\delta) - g(\theta))^2) \\
            &= \mathbb{E}_\theta((\delta - \mathbb{E}_\theta(\delta))^2) + \left( \mathbb{E}_\theta(\delta) - g(\theta)\right)^2 
                + 2 \mathbb{E}_\theta(\underbrace{(\delta - \mathbb{E}_\theta(\delta))}_{\text{d'esp. nulle}}) \left( \mathbb{E}_\theta(\delta) - g(\theta)\right) \\ 
            &= V_\theta(\delta) + b(\theta, \delta).
        \end{align*}
\end{proof}

\begin{proposition}
    Ainsi, sur $ \Delta_{sb}$, on aura $ \delta \succeq \delta'$ si $ R_Q(\theta, \delta) \leqslant R_Q(\theta, \delta')$ pour tout 
    $ \theta \in \Theta$. Ce qui équivaut donc à $ V_\theta( \delta) \leqslant \V_\theta( \delta')$. 
    Dire que $ \delta_{opt}$ est $\ell_Q$-optimal signifie que $ \delta_{opt}$ est \emph{sans biais de variance 
    minimale} pour tout $ \theta \in \Theta$. 
\end{proposition}

\subsection{Cas $q > 1$}

De même que précedement, on cherche à quantifier la pertience d'un estimateur en termes de dispersion 
et de précision par rapport au paramètre. On souhaite ici à estimer plusieurs paramètres 
$ \theta = (\theta_1, \dots, \theta_n)$. On notera alors un estimateur $ \delta(X_1, \dots, X_n) \in \R^q$ simplement 
$ \delta$ de coordonnées $ \delta^t = ( \delta_1, \dots, \delta_q)$. De même, on aura : 
    $$ \mathbb{E}_\theta( \delta) = ( \mathbb{E}_\theta(\delta_1), \dots, \mathbb{E}_\theta(\delta_q)) $$ 
    $$ \V_\theta( \delta) = \mathbb{E}_\theta \left[ (\delta - \mathbb{E}_\theta(\delta))
        ( \delta - \mathbb{E}_\theta(\delta)^t) \right] 
        = \Bigl( (\text{cov}_\theta(\delta_i, \delta_j)_{i,j}) \Bigr).
    $$

\begin{definition}[Perte, Risque, Comparaison - Cas Multivarié]
    Pour toute matrice $M$ semi-définie positive, on définit la perte $\ell$ d'un estimateur par rapport 
    à un vecteur paramètre $ \theta$ comme l'application telle que :
        $$ \ell_M(\theta, \delta) = (\delta - g(\theta))^t M (\delta - g(\theta)). $$
    Aussi le \emph{risque quadratique associé} à $M$ sera : 
        $$ \R_M(\theta, \delta) = \mathbb{E}_\theta(\ell_M(\theta, \delta)). $$
    Si $M = Id$, on retrouve alors le risque quadratique moyen. On dira ainsi que $ \delta \succeq_Q \delta'$ 
    (i.e il est $Q$-préférable à $ \delta'$) si : 
        $$ R_M( \theta, \delta) \leqslant R_M( \theta, \delta') \quad \forall M. $$
\end{definition}

\begin{theorem}[Caractérisation]
    On dira $ \delta \succeq_Q \delta'$ ssi la matrice de $ \mathcal{M}_q(\R)$ :
        $$ \boxed{
            \mathbb{E}_\theta(( \delta' - g( \theta)) ( \delta' - g( \theta))^t)
            - \mathbb{E}_\theta(( \delta - g( \theta))( \delta - g( \theta))^t)
        }
        $$
    est semi-définie positive. 
\end{theorem}

% \begin{proof}
%     On part, du lemme immédiat : 
%     \begin{quote}
%         \begin{lemma}
%             Il y a équivalence entre : 
%                 $$ 
%                     \forall \nu \in \R^q, \quad 
%                     \int_{y \in \R^q} ( \nu^t y)^2 \; d \myP(y) \leqslant \int_{y \in \R^q} ( \nu^t y)^2 \; d Q(y) 
%                 $$ 
%             et : 
%                 $$ 
%                     \forall A \in \mathcal{M}(q, q), \quad 
%                     \int_{y \in \R^q} y^t A y \; d \myP(y) \leqslant \int_{y \in \R^q} y^t A y \; d Q(y).
%                 $$
%             \emph{
%                 En effet, pour (1) $ \Longrightarrow $ (2) on pose $A = u u^t$ et pour 
%                 (2) $ \Longrightarrow$ (1), on utilise la décomposition de Gauss de A. 
%             }
%         \end{lemma}
%     \end{quote}

%     Donc dès que : 
%         $$ R_M( \theta, \delta) \leqslant R_M( \theta, \delta') $$
%     En posant : 
%     \begin{align*}
%         R_H( \theta, \delta) &= \int_{x \in \mathcal{X}^n} ( \delta(x) - g( \theta))^t M ( \delta(x) - g( \theta)) \; d \myP_\theta(x) \\ 
%         &= \int_{y \in \R^q} y^t M y \; d \myP_\theta^{ \delta - g( \theta)}(y)
%         \leqslant \int_{y \in \R^q} y^t M y \; d \myP_\theta ^{ \delta' - g( \theta)}(y)
%     \end{align*}
%     Ceci est donc équivalent à : 
%         \begin{align*}
%             \forall \nu \in \R^q, \quad & 
%             \int_{y \in \R^q} ( \nu^t y)^2 \; d \myP_\theta^{ \delta - g( \theta)}(y) 
%                 \leqslant \int^{y \in \R^q} ( \nu^t y)^2 \; d \myP_\theta^{ \delta' - g( \theta)} \\ 
%             \iff & \int_{x \in \mathcal{X}^n} \nu^t ( \delta(x) - g( \theta))^2 \; d \myP_\theta^n(x) 
%                 \leqslant \int^{y \in \R^q} ( \nu^t y)^2 \; d \myP_\theta^{ \delta' - g( \theta)} \\ 
%             \iff & \nu^t \left( \int_{x \in \mathcal{X}^n } ( \delta(x) - g( \theta)) ( \delta(x) - g( \theta))^t \; d \myP_\theta^n \right) u 
%                 \leqslant \int^{y \in \R^q} ( \nu^t y)^2 \; d \myP_\theta^{ \delta' - g( \theta)}. \\ 
%         \end{align*}
% \end{proof}

\subsection{Amélioration de Rao-Blackwell}

\begin{theorem}[Rao-Blackwell]
    Soit $ \delta \in L^2( \mathcal{P}_\Theta)$ sans biais (moyen) pour $g( \theta)$ et 
    $S$ une statistique exhaustive en $ \theta$. Alors, l'estimateur 
    $ \delta$ dit \emph{amélioré par} $S$, tel que $ \delta_S = \mathbb{E}_\theta( \delta \mid S)$ 
    est $Q$-préférable à $ \delta$. 
\end{theorem}

Donc pour réduire une variance dans $L^2$ sans perte d'information, il suffit de conditionner 
une statistique exhaustive. 

\begin{proof}
    $ \delta_S$ est un estimateur, car $S$ est exhaustive dont $ \mathbb{E}_\theta( \delta \mid S)$ 
    ne dépend pas de $ \theta$. 
    D'autre part, $ \delta_S$ est sans biais, puisque : 
        $$ \mathbb{E}_\theta( \mathbb{E}_\theta( \delta \mid S)) = \mathbb{E}_\theta( \delta) = g( \theta). $$
    Enfin, $ \delta_S$ est de moindre variance, comme $ \mathbb{E}_\theta( \delta \mid S)$ est 
    la projection dans $L^2( \myP_\theta)$. 
\end{proof}

\begin{remark}
    Dans le cas $ q = 1$, pour $ \Psi(x) = x^2$ convexe, on a : 
        \begin{align*}
            \mathbb{E}_\theta( ( \delta - g( \theta))^2 \mid S) & \geqslant \left[ \mathbb{E}_\theta( \delta - g( \theta)) \mid S\right]^2 \\ 
            &= [ \delta_S - g( \theta)]^2
        \end{align*}
    par l'inégalité de Jensen conditionnelle. Donc en prenant $ \mathbb{E}_\theta(.)$ à gauche 
    et à droite, on a : 
        \begin{align*}
            \mathbb{E}_\theta( ( \delta - g( \theta))^2) & \geqslant \mathbb{E}_\theta( \delta_S - g( \theta))^2 \\
            R_Q( \theta, \delta) & \geqslant R_Q( \theta, \delta_S).
        \end{align*}
    Donc conditionner par une statistique exhaustive améliore toujours la fonction 
    de risque. 
\end{remark}

\subsection{Optimalité de Lehman-Scheffe}

\begin{theorem}[Lehman-Scheffe]
    Si $ \delta$ est sans biais pour $ g( \theta)$ et $S$ exhaustive complète, alors 
    $ \delta_S = \mathbb{E}_\theta( \delta \mid S)$ est l'unique estimateur sans biais 
    de variance minimale (i.e l'optimal $ \delta_Q^{opt}$).
\end{theorem}

\begin{proof}
    Montrons que cet estimateur ne peut plus être amélioré. Soit $ \delta' \in \Delta_{sb}$, 
    posons $ \delta'_S = \mathbb{E}_\theta( \delta' \mid S)$. On a : 
        $$ \mathbb{E}_\theta( \delta') = \mathbb{E}_\theta( \delta) = g( \theta) $$
    Donc, pour tout $ \theta \in \Theta$ : 
        \begin{align*}
            \mathbb{E}_\theta( \underbrace{\delta'_S - \delta_S}_{= \varphi( \delta)}) = \mathbb{E}_\theta( \delta - \delta') = 0. 
        \end{align*}
    Avec $ \mathbb{E}_\theta( \varphi( S)) = 0$ pour tout $ \theta$. Or $S$ est complète donc 
    $ \varphi(S) = 0 $. D'où $ \delta'_S = \delta_S$. 
\end{proof}

\begin{proposition}
    Pour trouver le $ \delta_Q^{opt}$, il faut donc : 
    \begin{itemize}
        \item Trouver $S$ complète : on cherche $ \varphi (S)$ telle que $ \mathbb{E}_\theta( \varphi(S)) = g( \theta)$
            et la solution est bien l'estimateur sans biais de variance minimale. 
        \item On cherche $ \delta \in \Delta_{sb}$ simple. 
        \item Puis, on conditionne $ \mathbb{E}_\theta( \delta \mid S)$. Si $S$ 
            est complète, alors c'est gagné. 
    \end{itemize}
\end{proposition}


% ===================================================================================================================
% Bornes inférieures de variance

\section{Bornes inférieures de variance et Efficacité}

Nous avons précédemment défini $\Delta_{sb}$ comme la classe des estimateurs sans biais. 
Par la suite, nous avons introduit le risque quadratique pour quantifier la pertinence d'un estimateur en 
fonction de son biais et de sa variance. 

Bien que nous sachions construire des estimateurs sans biais, l'enjeu est désormais d'étudier leur variance afin de réduire 
leur dispersion autour du paramètre cible. Une question fondamentale se pose alors : au sein de $\Delta_{sb}$, 
existe-t-il un estimateur dont la variance est minimale de manière uniforme ? Si oui, comment peut-on le caractériser ?

Pour répondre à cette question, nous nous plaçons dans le cadre des modèles réguliers ($H_{1, 2, 3, 4}$). 
L'utilisation de l'information de Fisher comme mesure de précision nous impose toutefois d'ajouter des conditions 
sur la finitude de l'information ($H_5$) et sur la régularité de l'estimateur $\delta$ ($H_6$) pour permettre 
l'interversion intégrale-dérivée :

\begin{itemize}[label=--]
    \item[$(H_5)$] : L'information de Fisher est finie et non nulle :
    $$ \forall \theta \in \Theta, \quad \mathcal{I}_\theta(X) \in ]0, + \infty [ $$
    
    \item[$(H_6)$] : $\delta$ est de carré intégrable et permet la dérivation sous le signe somme :
    $$ \delta \in L^2( \mathcal{P}_\Theta), \forall \theta \in \Theta, \quad \mathbb{E}_\theta( \delta(X)) = g( \theta) \quad \text{ et } \quad \frac{\partial f_\theta}{\partial \theta} \delta \in L^1( \mu) $$
    Tels que pour tout $A \in \mathcal{A}$ et $\theta \in \Theta$ :
    $$ \frac{\partial}{\partial \theta} \left( \int_A  \delta f_\theta \; d \mu \right) = \int_A  \frac{\partial f_\theta}{\partial \theta} \delta \; d \mu $$
\end{itemize}

\subsection{Inégalité de Fréchet-Darmois-Cramér-Rao (FDCR)}

\begin{theorem}[Borne de Cramér-Rao]
    Sous les hypothèses de régularité usuelles ($H_{1 \dots 5}$) et pour un estimateur $\delta_n$ sans biais de $g(\theta)$, on a :
    \begin{equation}
        \boxed{\V_\theta( \delta_n) \geqslant \frac{(g'( \theta))^2}{n \mathcal{I}_\theta(X)}}
    \end{equation}
\end{theorem}

Plus l'information de Fisher est grande, plus la variance minimale possible est petite (le modèle est "précis").
Il convient alors de définir proprement les estimateurs atteingnant cette borne. 

\begin{definition}[Estimateur Efficace]
    Un estimateur $\delta_n$ est dit \emph{efficace} s'il est sans biais et qu'il \emph{atteint la borne} de Cramér-Rao :
        $$ \boxed{\V_\theta( \delta_n) = \frac{(g'(\theta))^2}{n \mathcal{I}_\theta(X)}} $$
\end{definition}

\subsection{La réciproque de Koopman (Cas d'égalité)}

\begin{prop}[Lien avec les modèles exponentiels]
    La borne de Cramér-Rao est atteinte (cas d'égalité) ssi le modèle est exponentiel de la forme :
    $$ f_\theta(x) = h(x) k_\theta \exp( \gamma(\theta) \delta(x)) $$
    Dans ce cas, $\delta(X)$ est l'unique estimateur efficace pour son espérance $g(\theta) = - \frac{\kappa_\theta'}{\gamma'(\theta)}$.
\end{prop}

\begin{example}[Application TD]
    Pour une loi de Poisson $\mathcal{P}(\theta)$, on montre que $\bar{X}_n$ est efficace car le modèle est exponentiel. Pour une loi Uniforme $\mathcal{U}[0, \theta]$, la borne ne s'applique pas (support dépendant de $\theta$) : l'estimateur peut avoir une variance \textit{plus petite} que la borne de Fisher.
\end{example}